/// <reference types="react" />
import { RouterPane, StructurePane } from '../types';
interface DeskToolPaneProps {
    group: RouterPane[];
    groupIndexes: number[];
    i: number;
    index: number;
    pane: StructurePane;
    paneKeys: string[];
    panes: StructurePane[];
    sibling: RouterPane;
    siblingIndex: number;
}
/**
 * @note The same pane might appear multiple times (split pane), so use index as tiebreaker
 *
 * @internal
 */
export declare function DeskToolPane(props: DeskToolPaneProps): JSX.Element;
export {};
//# sourceMappingURL=DeskToolPane.d.ts.map